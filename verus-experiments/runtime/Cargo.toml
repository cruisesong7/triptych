[package]
name = "triptych-verus-runtime"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vstd = "=0.0.0-2026-08-30-0159"

[package.metadata.verus]
verify = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
  "cfg(verus_only)",
  "cfg(verus_keep_ghost)",
] }
