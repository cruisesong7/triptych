/- Generated by Triptych from `triptych IPv4`. -/

import Triptych.Architecture.GeneratedLinter
import Triptych.Architecture.Denote
import Triptych.Architecture.Derivation
import Triptych.Architecture.Value
import Triptych.Architecture.Constraint
import Triptych.Architecture.Assemble
import Triptych.Theorems.Reconcile
import Triptych.Theorems.Value
import Triptych.Theorems.DecodeLemmas
import Triptych.Theorems.Derivation
import Outputs.IPv4.spec
import Inputs.IPv4

open Triptych
open CedarExamples.IPv4

set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unnecessarySeqFocus false

/- ══════════════════════════════ engine ══════════════════════════════
The executable counterpart of the spec. `decode` walks the grammar over an input
string and returns its captured components; `computeValue` then evaluates the value
function on those captures. Generated `DecidablePred` instances make the readable
`IsWf` and `IsValid` predicates directly executable.
`decodeView` packages the exact input and value/constraint captures as a typed `View`.

The public format API stays capitalized: use `#eval decide (IsValid s)` and
`#eval computeValue s`. The equivalence section below proves that these readable
predicates execute through the generic Triptych interpreter. -/

instance IPv4.Derivation.Oct1.instDecidableValid : DecidablePred IPv4.Derivation.Oct1.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.Oct1.Valid] <;> infer_instance

def IPv4.Derivation.Oct1.capturesWith (qual : String) : IPv4.Derivation.Oct1 → Triptych.CaptureMap
  | .alt0 part0 => ([] ++ [])

instance IPv4.Derivation.Oct2.instDecidableValid : DecidablePred IPv4.Derivation.Oct2.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.Oct2.Valid] <;> infer_instance

def IPv4.Derivation.Oct2.capturesWith (qual : String) : IPv4.Derivation.Oct2 → Triptych.CaptureMap
  | .alt0 part0 => ([] ++ [])

instance IPv4.Derivation.Oct3.instDecidableValid : DecidablePred IPv4.Derivation.Oct3.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.Oct3.Valid] <;> infer_instance

def IPv4.Derivation.Oct3.capturesWith (qual : String) : IPv4.Derivation.Oct3 → Triptych.CaptureMap
  | .alt0 part0 => ([] ++ [])

instance IPv4.Derivation.Oct4.instDecidableValid : DecidablePred IPv4.Derivation.Oct4.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.Oct4.Valid] <;> infer_instance

def IPv4.Derivation.Oct4.capturesWith (qual : String) : IPv4.Derivation.Oct4 → Triptych.CaptureMap
  | .alt0 part0 => ([] ++ [])

instance IPv4.Derivation.V4Addr.instDecidableValid : DecidablePred IPv4.Derivation.V4Addr.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.V4Addr.Valid] <;> infer_instance

def IPv4.Derivation.V4Addr.capturesWith (qual : String) : IPv4.Derivation.V4Addr → Triptych.CaptureMap
  | .alt0 part0 part2 part4 part6 =>
    (Triptych.referenceCaptures qual "Oct1" (IPv4.Derivation.Oct1.render part0) ++
        IPv4.Derivation.Oct1.capturesWith "Oct1" part0 ++
      ([] ++
        (Triptych.referenceCaptures qual "Oct2" (IPv4.Derivation.Oct2.render part2) ++
            IPv4.Derivation.Oct2.capturesWith "Oct2" part2 ++
          ([] ++
            (Triptych.referenceCaptures qual "Oct3" (IPv4.Derivation.Oct3.render part4) ++
                IPv4.Derivation.Oct3.capturesWith "Oct3" part4 ++
              ([] ++
                (Triptych.referenceCaptures qual "Oct4" (IPv4.Derivation.Oct4.render part6) ++
                    IPv4.Derivation.Oct4.capturesWith "Oct4" part6 ++
                  [])))))))

instance IPv4.Derivation.Prefix.instDecidableValid : DecidablePred IPv4.Derivation.Prefix.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.Prefix.Valid] <;> infer_instance

def IPv4.Derivation.Prefix.capturesWith (qual : String) : IPv4.Derivation.Prefix → Triptych.CaptureMap
  | .alt0 part0 => ([] ++ [])

instance IPv4.Derivation.V4Net.instDecidableValid : DecidablePred IPv4.Derivation.V4Net.Valid := fun d => by
  cases d <;> simp only [IPv4.Derivation.V4Net.Valid] <;> infer_instance

def IPv4.Derivation.V4Net.capturesWith (qual : String) : IPv4.Derivation.V4Net → Triptych.CaptureMap
  | .alt0 part0 =>
    (Triptych.referenceCaptures qual "V4Addr" (IPv4.Derivation.V4Addr.render part0) ++
        IPv4.Derivation.V4Addr.capturesWith "V4Addr" part0 ++
      [])
  | .alt1 part0 part2 =>
    (Triptych.referenceCaptures qual "V4Addr" (IPv4.Derivation.V4Addr.render part0) ++
        IPv4.Derivation.V4Addr.capturesWith "V4Addr" part0 ++
      ([] ++
        (Triptych.referenceCaptures qual "Prefix" (IPv4.Derivation.Prefix.render part2) ++
            IPv4.Derivation.Prefix.capturesWith "Prefix" part2 ++
          [])))

def IPv4.valueFn := fun m : Triptych.CaptureMap =>
  toIPv4Net ((Triptych.CaptureMap.toEnv m "Oct1").getD "") ((Triptych.CaptureMap.toEnv m "Oct2").getD "")
    ((Triptych.CaptureMap.toEnv m "Oct3").getD "") ((Triptych.CaptureMap.toEnv m "Oct4").getD "")
    ((Triptych.CaptureMap.toEnv m "Prefix").getD "")

def IPv4.constraints : List ConstraintEntry :=
  [ConstraintEntry.dsl ConstraintPhase.wellFormed (Constraint.noLeadingZero "Oct1"),
    ConstraintEntry.dsl ConstraintPhase.wellFormed
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Oct1"))
        (Constraint.le (ValExpr.nat "Oct1") (ValExpr.lit 255))),
    ConstraintEntry.dsl ConstraintPhase.wellFormed (Constraint.noLeadingZero "Oct2"),
    ConstraintEntry.dsl ConstraintPhase.wellFormed
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Oct2"))
        (Constraint.le (ValExpr.nat "Oct2") (ValExpr.lit 255))),
    ConstraintEntry.dsl ConstraintPhase.wellFormed (Constraint.noLeadingZero "Oct3"),
    ConstraintEntry.dsl ConstraintPhase.wellFormed
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Oct3"))
        (Constraint.le (ValExpr.nat "Oct3") (ValExpr.lit 255))),
    ConstraintEntry.dsl ConstraintPhase.wellFormed (Constraint.noLeadingZero "Oct4"),
    ConstraintEntry.dsl ConstraintPhase.wellFormed
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Oct4"))
        (Constraint.le (ValExpr.nat "Oct4") (ValExpr.lit 255))),
    ConstraintEntry.dsl ConstraintPhase.wellFormed (Constraint.noLeadingZero "Prefix"),
    ConstraintEntry.dsl ConstraintPhase.wellFormed
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Prefix"))
        (Constraint.le (ValExpr.nat "Prefix") (ValExpr.lit 32)))]

def IPv4.computeValue (s : String) :=
  Triptych.computeValueMap IPv4.grammar IPv4.valueFn s

def IPv4.View.ofMap (input : String) (m : Triptych.CaptureMap) : IPv4.View :=
  IPv4.View.mk input ((Triptych.CaptureMap.toEnv m "Oct1").getD "") ((Triptych.CaptureMap.toEnv m "Oct2").getD "")
    ((Triptych.CaptureMap.toEnv m "Oct3").getD "") ((Triptych.CaptureMap.toEnv m "Oct4").getD "")
    (Triptych.CaptureMap.toEnv m "Prefix")

def IPv4.decodeView (s : String) : Option IPv4.View :=
  (decode IPv4.grammar s).map (IPv4.View.ofMap s)

def IPv4.Derivation.V4Net.toView (d : IPv4.Derivation.V4Net) : IPv4.View :=
  IPv4.View.ofMap (IPv4.Derivation.V4Net.render d) (IPv4.Derivation.V4Net.capturesWith "" d)

/- ════════════════════════════ equivalence ════════════════════════════
The auto-discharged guarantees relating the readable surface to the executable
engine: `IsWfGrammar_equiv` proves grammar-layout agreement and `IsWf_equiv`
proves full well-formedness agreement. `computeValue_eq` proves the values agree,
while `decodeView_input`, `IsValid_view`, and `computeValue_view` expose the typed
parse view. Derived `DecidablePred` instances make the surface predicates executable
via the engine. No `sorry`. -/

theorem IPv4.Derivation.Oct1.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.Oct1)
    (hvalid : IPv4.Derivation.Oct1.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 1)
      (Production.mk "Oct1" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      (IPv4.Derivation.Oct1.render d) (IPv4.Derivation.Oct1.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 =>
    simp only [IPv4.Derivation.Oct1.Valid] at hvalid
    have hpart0 := hvalid
    simp only [IPv4.Derivation.Oct1.render, IPv4.Derivation.Oct1.capturesWith]
    apply Triptych.prodMatch_of_alt (alt := [SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          · exact Triptych.symMatch_term IPv4.grammar qual (fuel + 1) TokClass.digit (LenSpec.between 1 3) part0 hpart0
          · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 1))

theorem IPv4.Derivation.Oct2.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.Oct2)
    (hvalid : IPv4.Derivation.Oct2.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 1)
      (Production.mk "Oct2" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      (IPv4.Derivation.Oct2.render d) (IPv4.Derivation.Oct2.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 =>
    simp only [IPv4.Derivation.Oct2.Valid] at hvalid
    have hpart0 := hvalid
    simp only [IPv4.Derivation.Oct2.render, IPv4.Derivation.Oct2.capturesWith]
    apply Triptych.prodMatch_of_alt (alt := [SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          · exact Triptych.symMatch_term IPv4.grammar qual (fuel + 1) TokClass.digit (LenSpec.between 1 3) part0 hpart0
          · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 1))

theorem IPv4.Derivation.Oct3.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.Oct3)
    (hvalid : IPv4.Derivation.Oct3.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 1)
      (Production.mk "Oct3" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      (IPv4.Derivation.Oct3.render d) (IPv4.Derivation.Oct3.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 =>
    simp only [IPv4.Derivation.Oct3.Valid] at hvalid
    have hpart0 := hvalid
    simp only [IPv4.Derivation.Oct3.render, IPv4.Derivation.Oct3.capturesWith]
    apply Triptych.prodMatch_of_alt (alt := [SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          · exact Triptych.symMatch_term IPv4.grammar qual (fuel + 1) TokClass.digit (LenSpec.between 1 3) part0 hpart0
          · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 1))

theorem IPv4.Derivation.Oct4.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.Oct4)
    (hvalid : IPv4.Derivation.Oct4.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 1)
      (Production.mk "Oct4" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      (IPv4.Derivation.Oct4.render d) (IPv4.Derivation.Oct4.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 =>
    simp only [IPv4.Derivation.Oct4.Valid] at hvalid
    have hpart0 := hvalid
    simp only [IPv4.Derivation.Oct4.render, IPv4.Derivation.Oct4.capturesWith]
    apply Triptych.prodMatch_of_alt (alt := [SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          · exact Triptych.symMatch_term IPv4.grammar qual (fuel + 1) TokClass.digit (LenSpec.between 1 3) part0 hpart0
          · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 1))

theorem IPv4.Derivation.V4Addr.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.V4Addr)
    (hvalid : IPv4.Derivation.V4Addr.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 2)
      (Production.mk "V4Addr"
        [[SymItem.mk (Sym.ref "Oct1") false, SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "Oct2") false,
            SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "Oct3") false, SymItem.mk (Sym.lit ".") false,
            SymItem.mk (Sym.ref "Oct4") false]])
      (IPv4.Derivation.V4Addr.render d) (IPv4.Derivation.V4Addr.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 part2 part4 part6 =>
    simp only [IPv4.Derivation.V4Addr.Valid] at hvalid
    rcases hvalid with ⟨hpart0, hpart2, hpart4, hpart6⟩
    simp only [IPv4.Derivation.V4Addr.render, IPv4.Derivation.V4Addr.capturesWith]
    apply
      Triptych.prodMatch_of_alt (alt :=
        [SymItem.mk (Sym.ref "Oct1") false, SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "Oct2") false,
          SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "Oct3") false, SymItem.mk (Sym.lit ".") false,
          SymItem.mk (Sym.ref "Oct4") false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          ·
            exact
              (by
                simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                  (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 0)) + 1) "Oct1"
                    (Production.mk "Oct1" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]]) (by rfl)
                    (IPv4.Derivation.Oct1.render part0) (IPv4.Derivation.Oct1.capturesWith "Oct1" part0)
                    (IPv4.Derivation.Oct1.matches "Oct1" (fuel + 0) part0 hpart0)))
          ·
            exact
              (by
                apply Triptych.seqMatch_required
                · exact Triptych.symMatch_lit IPv4.grammar qual (fuel + 2) "."
                ·
                  exact
                    (by
                      apply Triptych.seqMatch_required
                      ·
                        exact
                          (by
                            simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                              (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 0)) + 1) "Oct2"
                                (Production.mk "Oct2"
                                  [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
                                (by rfl) (IPv4.Derivation.Oct2.render part2)
                                (IPv4.Derivation.Oct2.capturesWith "Oct2" part2)
                                (IPv4.Derivation.Oct2.matches "Oct2" (fuel + 0) part2 hpart2)))
                      ·
                        exact
                          (by
                            apply Triptych.seqMatch_required
                            · exact Triptych.symMatch_lit IPv4.grammar qual (fuel + 2) "."
                            ·
                              exact
                                (by
                                  apply Triptych.seqMatch_required
                                  ·
                                    exact
                                      (by
                                        simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                                          (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 0)) + 1) "Oct3"
                                            (Production.mk "Oct3"
                                              [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
                                            (by rfl) (IPv4.Derivation.Oct3.render part4)
                                            (IPv4.Derivation.Oct3.capturesWith "Oct3" part4)
                                            (IPv4.Derivation.Oct3.matches "Oct3" (fuel + 0) part4 hpart4)))
                                  ·
                                    exact
                                      (by
                                        apply Triptych.seqMatch_required
                                        · exact Triptych.symMatch_lit IPv4.grammar qual (fuel + 2) "."
                                        ·
                                          exact
                                            (by
                                              apply Triptych.seqMatch_required
                                              ·
                                                exact
                                                  (by
                                                    simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                                                      (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 0)) + 1) "Oct4"
                                                        (Production.mk "Oct4"
                                                          [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3))
                                                                false]])
                                                        (by rfl) (IPv4.Derivation.Oct4.render part6)
                                                        (IPv4.Derivation.Oct4.capturesWith "Oct4" part6)
                                                        (IPv4.Derivation.Oct4.matches "Oct4" (fuel + 0) part6 hpart6)))
                                              · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 2))))))))

theorem IPv4.Derivation.Prefix.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.Prefix)
    (hvalid : IPv4.Derivation.Prefix.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 1)
      (Production.mk "Prefix" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 2)) false]])
      (IPv4.Derivation.Prefix.render d) (IPv4.Derivation.Prefix.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 =>
    simp only [IPv4.Derivation.Prefix.Valid] at hvalid
    have hpart0 := hvalid
    simp only [IPv4.Derivation.Prefix.render, IPv4.Derivation.Prefix.capturesWith]
    apply Triptych.prodMatch_of_alt (alt := [SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 2)) false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          · exact Triptych.symMatch_term IPv4.grammar qual (fuel + 1) TokClass.digit (LenSpec.between 1 2) part0 hpart0
          · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 1))

theorem IPv4.Derivation.V4Net.matches (qual : String) (fuel : Nat) (d : IPv4.Derivation.V4Net)
    (hvalid : IPv4.Derivation.V4Net.Valid d) :
    Triptych.ProdMatch IPv4.grammar qual (fuel + 3)
      (Production.mk "V4Net"
        [[SymItem.mk (Sym.ref "V4Addr") false],
          [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false, SymItem.mk (Sym.ref "Prefix") false]])
      (IPv4.Derivation.V4Net.render d) (IPv4.Derivation.V4Net.capturesWith qual d) :=
  by
  cases d with
  | alt0 part0 =>
    simp only [IPv4.Derivation.V4Net.Valid] at hvalid
    have hpart0 := hvalid
    simp only [IPv4.Derivation.V4Net.render, IPv4.Derivation.V4Net.capturesWith]
    apply Triptych.prodMatch_of_alt (alt := [SymItem.mk (Sym.ref "V4Addr") false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          ·
            exact
              (by
                simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                  (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 0)) + 2) "V4Addr"
                    (Production.mk "V4Addr"
                      [[SymItem.mk (Sym.ref "Oct1") false, SymItem.mk (Sym.lit ".") false,
                          SymItem.mk (Sym.ref "Oct2") false, SymItem.mk (Sym.lit ".") false,
                          SymItem.mk (Sym.ref "Oct3") false, SymItem.mk (Sym.lit ".") false,
                          SymItem.mk (Sym.ref "Oct4") false]])
                    (by rfl) (IPv4.Derivation.V4Addr.render part0) (IPv4.Derivation.V4Addr.capturesWith "V4Addr" part0)
                    (IPv4.Derivation.V4Addr.matches "V4Addr" (fuel + 0) part0 hpart0)))
          · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 3))
  | alt1 part0 part2 =>
    simp only [IPv4.Derivation.V4Net.Valid] at hvalid
    rcases hvalid with ⟨hpart0, hpart2⟩
    simp only [IPv4.Derivation.V4Net.render, IPv4.Derivation.V4Net.capturesWith]
    apply
      Triptych.prodMatch_of_alt (alt :=
        [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false, SymItem.mk (Sym.ref "Prefix") false])
    · simp
    ·
      exact
        (by
          apply Triptych.seqMatch_required
          ·
            exact
              (by
                simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                  (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 0)) + 2) "V4Addr"
                    (Production.mk "V4Addr"
                      [[SymItem.mk (Sym.ref "Oct1") false, SymItem.mk (Sym.lit ".") false,
                          SymItem.mk (Sym.ref "Oct2") false, SymItem.mk (Sym.lit ".") false,
                          SymItem.mk (Sym.ref "Oct3") false, SymItem.mk (Sym.lit ".") false,
                          SymItem.mk (Sym.ref "Oct4") false]])
                    (by rfl) (IPv4.Derivation.V4Addr.render part0) (IPv4.Derivation.V4Addr.capturesWith "V4Addr" part0)
                    (IPv4.Derivation.V4Addr.matches "V4Addr" (fuel + 0) part0 hpart0)))
          ·
            exact
              (by
                apply Triptych.seqMatch_required
                · exact Triptych.symMatch_lit IPv4.grammar qual (fuel + 3) "/"
                ·
                  exact
                    (by
                      apply Triptych.seqMatch_required
                      ·
                        exact
                          (by
                            simpa only [Nat.add_assoc, Nat.add_comm, Nat.add_left_comm] using
                              (Triptych.symMatch_ref IPv4.grammar qual (((fuel + 1)) + 1) "Prefix"
                                (Production.mk "Prefix"
                                  [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 2)) false]])
                                (by rfl) (IPv4.Derivation.Prefix.render part2)
                                (IPv4.Derivation.Prefix.capturesWith "Prefix" part2)
                                (IPv4.Derivation.Prefix.matches "Prefix" (fuel + 1) part2 hpart2)))
                      · exact Triptych.seqMatch_nil IPv4.grammar qual (fuel + 3))))

theorem IPv4.Derivation.V4Net.mem_fullParses (d : IPv4.Derivation.V4Net) (hvalid : IPv4.Derivation.V4Net.Valid d) :
    IPv4.Derivation.V4Net.capturesWith "" d ∈ Triptych.fullParses IPv4.grammar (IPv4.Derivation.V4Net.render d) :=
  by
  apply
    Triptych.prodMatch_mem_fullParses IPv4.grammar
      (Production.mk "V4Net"
        [[SymItem.mk (Sym.ref "V4Addr") false],
          [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false, SymItem.mk (Sym.ref "Prefix") false]])
      (by rfl)
  simpa [IPv4.grammar] using (IPv4.Derivation.V4Net.matches "" 4 d hvalid)

theorem IPv4.Derivation.V4Net.decode_render_of_captureFunctional
    (hfunctional : Triptych.GrammarCaptureFunctional IPv4.grammar) (d : IPv4.Derivation.V4Net)
    (hvalid : IPv4.Derivation.V4Net.Valid d) :
    Triptych.decode IPv4.grammar (IPv4.Derivation.V4Net.render d) = some (IPv4.Derivation.V4Net.capturesWith "" d) :=
  Triptych.decode_eq_of_mem_fullParses hfunctional (mem_fullParses d hvalid)

theorem IPv4.decodeView_input {s : String} {v : IPv4.View} (h : IPv4.decodeView s = some v) : IPv4.View.input v = s :=
  by
  unfold IPv4.decodeView at h
  rw [Option.map_eq_some_iff] at h
  obtain ⟨m, _, hv⟩ := h
  subst v
  rfl

theorem IPv4.Derivation.V4Net.decodeView_render_of_captureFunctional
    (hfunctional : Triptych.GrammarCaptureFunctional IPv4.grammar) (d : IPv4.Derivation.V4Net)
    (hvalid : IPv4.Derivation.V4Net.Valid d) :
    IPv4.decodeView (IPv4.Derivation.V4Net.render d) = some (IPv4.Derivation.V4Net.toView d) :=
  by
  unfold IPv4.decodeView IPv4.Derivation.V4Net.toView
  rw [IPv4.Derivation.V4Net.decode_render_of_captureFunctional hfunctional d hvalid]
  rfl

theorem IPv4.SatisfiesWfConstraints_of_decode {s : String} {m : Triptych.CaptureMap}
    (h : decode IPv4.grammar s = some m) :
    IPv4.SatisfiesWfConstraints s ↔
      IPv4.WfConstraints ((Triptych.CaptureMap.toEnv m "Oct1").getD "") ((Triptych.CaptureMap.toEnv m "Oct2").getD "")
        ((Triptych.CaptureMap.toEnv m "Oct3").getD "") ((Triptych.CaptureMap.toEnv m "Oct4").getD "")
        ((Triptych.CaptureMap.toEnv m "Prefix").getD "") :=
  by
  unfold IPv4.SatisfiesWfConstraints
  simp only [Triptych.component_eq_of_decode h "Oct1", Triptych.component_eq_of_decode h "Oct2",
    Triptych.component_eq_of_decode h "Oct3", Triptych.component_eq_of_decode h "Oct4",
    Triptych.component_eq_of_decode h "Prefix"]

theorem IPv4.Internal.matchesRef.Oct1 (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 1) (Sym.ref "Oct1") s ↔ IPv4.IsWf.Oct1 s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "Oct1" =
        some (Production.mk "Oct1" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold IPv4.IsWf.Oct1
  simp (config := { maxSteps := 1000000 }) only [Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem IPv4.Internal.matchesRef.Oct2 (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 1) (Sym.ref "Oct2") s ↔ IPv4.IsWf.Oct2 s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "Oct2" =
        some (Production.mk "Oct2" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold IPv4.IsWf.Oct2
  simp (config := { maxSteps := 1000000 }) only [Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem IPv4.Internal.matchesRef.Oct3 (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 1) (Sym.ref "Oct3") s ↔ IPv4.IsWf.Oct3 s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "Oct3" =
        some (Production.mk "Oct3" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold IPv4.IsWf.Oct3
  simp (config := { maxSteps := 1000000 }) only [Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem IPv4.Internal.matchesRef.Oct4 (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 1) (Sym.ref "Oct4") s ↔ IPv4.IsWf.Oct4 s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "Oct4" =
        some (Production.mk "Oct4" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 3)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold IPv4.IsWf.Oct4
  simp (config := { maxSteps := 1000000 }) only [Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem IPv4.Internal.matchesRef.V4Addr (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 2) (Sym.ref "V4Addr") s ↔ IPv4.IsWf.V4Addr s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "V4Addr" =
        some
          (Production.mk "V4Addr"
            [[SymItem.mk (Sym.ref "Oct1") false, SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "Oct2") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "Oct3") false, SymItem.mk (Sym.lit ".") false,
                SymItem.mk (Sym.ref "Oct4") false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold IPv4.IsWf.V4Addr
  simp (config := { maxSteps := 1000000 }) only [Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym,
    IPv4.Internal.matchesRef.Oct1, IPv4.Internal.matchesRef.Oct2, IPv4.Internal.matchesRef.Oct3,
    IPv4.Internal.matchesRef.Oct4]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem IPv4.Internal.matchesRef.Prefix (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 1) (Sym.ref "Prefix") s ↔ IPv4.IsWf.Prefix s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "Prefix" =
        some (Production.mk "Prefix" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.between 1 2)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold IPv4.IsWf.Prefix
  simp (config := { maxSteps := 1000000 }) only [Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem IPv4.Internal.matchesRef.V4Net (fuel : Nat) (s : String) :
    matchesSym IPv4.grammar (fuel + 3) (Sym.ref "V4Net") s ↔ IPv4.IsWf.V4Net s :=
  by
  rw [matchesSym,
    show
      (IPv4.grammar).prod? "V4Net" =
        some
          (Production.mk "V4Net"
            [[SymItem.mk (Sym.ref "V4Addr") false],
              [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false,
                SymItem.mk (Sym.ref "Prefix") false]])
      from rfl]
  dsimp only
  unfold matchesProd IPv4.IsWf.V4Net
  simp (config := { maxSteps := 1000000 }) only [List.mem_cons, List.mem_singleton, List.not_mem_nil,
    Triptych.matchesSeq.eq_1, Triptych.matchesSeq.eq_2, exists_eq_or_imp, exists_eq_left, exists_eq_left, if_true,
    if_false, Bool.false_eq_true, false_and, or_false, or_assoc, Triptych.matchesSym, IPv4.Internal.matchesRef.V4Addr,
    IPv4.Internal.matchesRef.Prefix]
  repeat'
    first
    | apply or_congr
    | ( simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left,
          ← and_assoc, exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
        try grind [String.append_assoc, String.append_empty])

theorem IPv4.IsWfGrammar_equiv (s : String) : Triptych.IsWf IPv4.grammar s ↔ IPv4.IsWf.V4Net s :=
  by
  rw [isWf_eq_isWfProd_start, IsWfProd,
    show
      (IPv4.grammar).prod? (IPv4.grammar).start =
        some
          (Production.mk "V4Net"
            [[SymItem.mk (Sym.ref "V4Addr") false],
              [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false,
                SymItem.mk (Sym.ref "Prefix") false]])
      from rfl]
  have hstart :
    ∀ n,
      matchesProd IPv4.grammar n
          (Production.mk "V4Net"
            [[SymItem.mk (Sym.ref "V4Addr") false],
              [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false,
                SymItem.mk (Sym.ref "Prefix") false]])
          s =
        matchesSym IPv4.grammar (n + 1) (Sym.ref "V4Net") s :=
    by
    intro n
    rw [matchesSym,
      show
        (IPv4.grammar).prod? "V4Net" =
          some
            (Production.mk "V4Net"
              [[SymItem.mk (Sym.ref "V4Addr") false],
                [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false,
                  SymItem.mk (Sym.ref "Prefix") false]])
        from rfl]
  show
    matchesProd IPv4.grammar (IPv4.grammar).prods.length
        (Production.mk "V4Net"
          [[SymItem.mk (Sym.ref "V4Addr") false],
            [SymItem.mk (Sym.ref "V4Addr") false, SymItem.mk (Sym.lit "/") false, SymItem.mk (Sym.ref "Prefix") false]])
        s ↔
      _
  rw [hstart]
  exact IPv4.Internal.matchesRef.V4Net _ s

theorem IPv4.IsWf_equiv (s : String) : IPv4.IsWf s ↔ Triptych.isWf IPv4.grammar IPv4.constraints s :=
  by
  unfold IPv4.IsWf Triptych.isWf
  rw [← IPv4.IsWfGrammar_equiv, ← decodeSome_iff_IsWf IPv4.grammar (by decide)]
  unfold IPv4.SatisfiesWfConstraints IPv4.WfConstraints IPv4.constraints
  simp only [Triptych.component, Triptych.componentList, Triptych.envOf, Triptych.captureMapOf, List.forall_mem_cons,
    List.forall_mem_singleton, List.not_mem_nil, forall_const, ConstraintEntry.wfPart, Constraint.eval, ValExpr.eval,
    presentCount, natOf_getD, intOf_getD, lenOf_getD, countOf_getD, signOf_getD, Env.countVal, and_true, true_and,
    false_implies, implies_true, Bool.false_eq_true]
  try grind

theorem IPv4.IsValid_equiv (s : String) :
    IPv4.IsValid s ↔
      Triptych.isWf IPv4.grammar IPv4.constraints s ∧ Triptych.satisfiesConstraints IPv4.grammar IPv4.constraints s :=
  by
  unfold IPv4.IsValid
  rw [(IPv4.IsWf_equiv s)]
  unfold Triptych.satisfiesConstraints Triptych.captureMapOf IPv4.constraints
  simp only [List.forall_mem_cons, List.forall_mem_singleton, List.not_mem_nil, ConstraintEntry.valPart, false_implies,
    true_and, and_true]
  constructor
  · intro h
    exact ⟨h, fun _ => True.intro⟩
  · exact And.left

instance IPv4.instDecidableGrammar : DecidablePred IPv4.IsWf.V4Net := fun s =>
  @decidable_of_iff _ _ (IPv4.IsWfGrammar_equiv s) (Triptych.decIsWf IPv4.grammar (by decide) s)

instance IPv4.instDecidableIsWf : DecidablePred IPv4.IsWf := fun s =>
  @decidable_of_iff _ _ (IPv4.IsWf_equiv s).symm inferInstance

instance IPv4.instDecidableIsValid : DecidablePred IPv4.IsValid := fun s => inferInstance

theorem IPv4.View.wfConstraints_of_decode {s : String} {m : Triptych.CaptureMap} (h : decode IPv4.grammar s = some m) :
    IPv4.SatisfiesWfConstraints s ↔ IPv4.View.WfConstraints (IPv4.View.ofMap s m) :=
  by
  rw [IPv4.SatisfiesWfConstraints_of_decode h]
  rfl

theorem IPv4.IsValid_view (s : String) :
    IPv4.IsValid s ↔ ∃ v : IPv4.View, IPv4.decodeView s = some v ∧ IPv4.View.Valid v :=
  by
  constructor
  · intro hvalid
    have hengine := (IPv4.IsValid_equiv s).mp hvalid
    have hsome : (decode IPv4.grammar s).isSome = true := by exact hengine.1.1
    obtain ⟨m, hm⟩ := Option.isSome_iff_exists.mp hsome
    refine ⟨IPv4.View.ofMap s m, ?_, ?_⟩
    · unfold IPv4.decodeView
      simp [hm]
    · exact (IPv4.View.wfConstraints_of_decode hm).mp hvalid.2
  · rintro ⟨v, hview, hvalid⟩
    unfold IPv4.decodeView at hview
    rw [Option.map_eq_some_iff] at hview
    obtain ⟨m, hm, hv⟩ := hview
    subst v
    have hdecoded : (decode IPv4.grammar s).isSome = true := by simp [hm]
    have hgrammar := (IPv4.IsWfGrammar_equiv s).mp ((decodeSome_iff_IsWf IPv4.grammar (by decide) s).mp hdecoded)
    exact ⟨hgrammar, (IPv4.View.wfConstraints_of_decode hm).mpr hvalid⟩

theorem IPv4.computeValue_eq (s : String) :
    IPv4.computeValue s =
      (decode IPv4.grammar s).map
        (fun _ =>
          IPv4.value (Triptych.component IPv4.grammar s "Oct1") (Triptych.component IPv4.grammar s "Oct2")
            (Triptych.component IPv4.grammar s "Oct3") (Triptych.component IPv4.grammar s "Oct4")
            (Triptych.component IPv4.grammar s "Prefix")) :=
  by
  unfold IPv4.computeValue Triptych.computeValueMap Triptych.component Triptych.envOf Triptych.captureMapOf IPv4.value
    IPv4.valueFn
  cases h : decode IPv4.grammar s with
  | none => simp
  | some m => simp [natOf_getD, intOf_getD, lenOf_getD, countOf_getD, signOf_getD, Env.countVal, ValExpr.eval]

theorem IPv4.computeValue_of_decode {s : String} {m : Triptych.CaptureMap} (h : decode IPv4.grammar s = some m) :
    IPv4.computeValue s =
      some
        (IPv4.value ((Triptych.CaptureMap.toEnv m "Oct1").getD "") ((Triptych.CaptureMap.toEnv m "Oct2").getD "")
          ((Triptych.CaptureMap.toEnv m "Oct3").getD "") ((Triptych.CaptureMap.toEnv m "Oct4").getD "")
          ((Triptych.CaptureMap.toEnv m "Prefix").getD "")) :=
  by
  rw [IPv4.computeValue_eq, h]
  simp only [Option.map_some, Triptych.component_eq_of_decode h "Oct1", Triptych.component_eq_of_decode h "Oct2",
    Triptych.component_eq_of_decode h "Oct3", Triptych.component_eq_of_decode h "Oct4",
    Triptych.component_eq_of_decode h "Prefix"]

theorem IPv4.computeValue_view (s : String) : IPv4.computeValue s = (IPv4.decodeView s).map IPv4.View.denotation :=
  by
  unfold IPv4.decodeView
  cases h : decode IPv4.grammar s with
  |
    none =>
    rw [show IPv4.computeValue s = none by
        rw [IPv4.computeValue_eq, h]
        rfl]
    rfl
  | some m =>
    rw [IPv4.computeValue_of_decode h]
    simp only [Option.map_some]
    rfl

/- ═══════════════════════════════ parser ══════════════════════════════
The generated correct-by-construction parser `parse` (= `computeValue` gated on the
decidable `IsValid`) together with its guarantees — `parse_sound`, `parse_complete`,
`parse_reject`, `parse_view`, and typed `parse_eq_some_iff_view` /
`parse_eq_none_iff_view` normal forms — all AUTO-DISCHARGED here.
When an external parser is declared, `checkedExtParse` additionally validates each
external result against this parser and ships an AUTO-DISCHARGED soundness theorem.
A verified parser, no `sorry`. -/

theorem IPv4.computeValue_isSome (s : String) : IPv4.IsValid s → (IPv4.computeValue s).isSome :=
  by
  intro h
  have hengine := (IPv4.IsValid_equiv s).mp h
  unfold Triptych.isWf at hengine
  unfold IPv4.computeValue Triptych.computeValueMap
  rw [Option.isSome_map]
  exact hengine.1.1

def IPv4.parse (s : String) :=
  Triptych.gatedParse IPv4.IsValid IPv4.computeValue s

theorem IPv4.parse_sound (s : String) (i : IPv4Net) :
    IPv4.parse s = some i → IPv4.IsValid s ∧ IPv4.computeValue s = some i :=
  Triptych.gatedParse_sound _ _ s i

theorem IPv4.parse_complete (s : String) (i : IPv4Net) :
    IPv4.IsValid s → IPv4.computeValue s = some i → IPv4.parse s = some i :=
  Triptych.gatedParse_complete _ _ s i

theorem IPv4.parse_reject (s : String) : IPv4.parse s = none ↔ ¬IPv4.IsValid s :=
  Triptych.gatedParse_reject _ _ IPv4.computeValue_isSome s

theorem IPv4.parse_view (s : String) :
    IPv4.parse s = if decide (IPv4.IsValid s) then (IPv4.decodeView s).map IPv4.View.denotation else none :=
  by
  unfold IPv4.parse Triptych.gatedParse
  rw [IPv4.computeValue_view]

theorem IPv4.parse_eq_some_iff_view (s : String) (i : IPv4Net) :
    IPv4.parse s = some i ↔
      ∃ v : IPv4.View, IPv4.decodeView s = some v ∧ IPv4.View.Valid v ∧ IPv4.View.denotation v = i :=
  by
  constructor
  · intro hparse
    obtain ⟨hvalid, hvalue⟩ := IPv4.parse_sound s i hparse
    obtain ⟨v, hview, hvalidView⟩ := (IPv4.IsValid_view s).mp hvalid
    refine ⟨v, hview, hvalidView, ?_⟩
    rw [IPv4.computeValue_view, hview] at hvalue
    exact Option.some.inj hvalue
  · rintro ⟨v, hview, hvalidView, hvalue⟩
    apply IPv4.parse_complete s i
    · exact (IPv4.IsValid_view s).mpr ⟨v, hview, hvalidView⟩
    · rw [IPv4.computeValue_view, hview]
      exact congrArg some hvalue

theorem IPv4.parse_eq_none_iff_view (s : String) :
    IPv4.parse s = none ↔ ¬∃ v : IPv4.View, IPv4.decodeView s = some v ∧ IPv4.View.Valid v :=
  by
  rw [IPv4.parse_reject]
  constructor
  · intro hinvalid ⟨v, hview, hvalidView⟩
    exact hinvalid ((IPv4.IsValid_view s).mpr ⟨v, hview, hvalidView⟩)
  · intro hnoview hvalid
    exact hnoview ((IPv4.IsValid_view s).mp hvalid)

def IPv4.checkedExtParse (s : String) : Option IPv4Net :=
  Triptych.checkedExternalParse IPv4.IsValid IPv4.computeValue ipv4Only id s

theorem IPv4.checkedExtParse_eq_some_iff (s : String) (i : IPv4Net) :
    IPv4.checkedExtParse s = some i ↔ ipv4Only s = some i ∧ IPv4.IsValid s ∧ IPv4.computeValue s = some (id i) :=
  Triptych.checkedExternalParse_eq_some_iff IPv4.IsValid IPv4.computeValue ipv4Only id s i

theorem IPv4.checkedExtParse_sound (s : String) (i : IPv4Net) :
    IPv4.checkedExtParse s = some i → IPv4.IsValid s ∧ IPv4.computeValue s = some (id i) :=
  Triptych.checkedExternalParse_sound IPv4.IsValid IPv4.computeValue ipv4Only id s i

theorem IPv4.checkedExtParse_sound_view (s : String) (i : IPv4Net) :
    IPv4.checkedExtParse s = some i →
      ∃ v : IPv4.View, IPv4.decodeView s = some v ∧ IPv4.View.Valid v ∧ IPv4.View.denotation v = id i :=
  by
  intro hparse
  obtain ⟨hvalid, hvalue⟩ := IPv4.checkedExtParse_sound s i hparse
  obtain ⟨v, hview, hvalidView⟩ := (IPv4.IsValid_view s).mp hvalid
  refine ⟨v, hview, hvalidView, ?_⟩
  rw [IPv4.computeValue_view, hview] at hvalue
  exact Option.some.inj hvalue
