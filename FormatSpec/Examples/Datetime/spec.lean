-- Generated by FormatSpec from `format_spec Datetime`. Do not edit by hand.

import FormatSpec.Denote
import FormatSpec.Value
import FormatSpec.Constraint
import FormatSpec.Assemble
import FormatSpec.Reconcile
import FormatSpec.Examples.Datetime.grammar

open FormatSpec
open FormatSpec.Examples.Datetime

set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false

-- ═══════════════════════════════ spec ═══════════════════════════════
-- The reader-facing specification: the grammar, the readable per-production
-- well-formedness predicates `IsWf.*`, the `value` function, the `Constraints`,
-- and the acceptance predicates `SatisfiesConstraints` / `IsValid` (a string is
-- VALID iff it satisfies the grammar and constraints — Cedar's wording).

def Datetime.grammar : Grammar :=
  Grammar.mk "Datetime"
    [Production.mk "Datetime"
        [[SymItem.mk (Sym.ref "Date") false],
          [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
            SymItem.mk (Sym.lit "Z") false],
          [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
            SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.lit "Z") false],
          [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
            SymItem.mk (Sym.ref "Offset") false],
          [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
            SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.ref "Offset") false]],
      Production.mk "Date"
        [[SymItem.mk (Sym.ref "YYYY") false, SymItem.mk (Sym.lit "-") false, SymItem.mk (Sym.ref "MM") false,
            SymItem.mk (Sym.lit "-") false, SymItem.mk (Sym.ref "DD") false]],
      Production.mk "Time"
        [[SymItem.mk (Sym.ref "hh") false, SymItem.mk (Sym.lit ":") false, SymItem.mk (Sym.ref "mm") false,
            SymItem.mk (Sym.lit ":") false, SymItem.mk (Sym.ref "ss") false]],
      Production.mk "Offset"
        [[SymItem.mk (Sym.lit "+") false, SymItem.mk (Sym.ref "hh") false, SymItem.mk (Sym.ref "mm") false],
          [SymItem.mk (Sym.lit "-") false, SymItem.mk (Sym.ref "hh") false, SymItem.mk (Sym.ref "mm") false]],
      Production.mk "SSS" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 3)) false]],
      Production.mk "YYYY" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 4)) false]],
      Production.mk "MM" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]],
      Production.mk "DD" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]],
      Production.mk "hh" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]],
      Production.mk "mm" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]],
      Production.mk "ss" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]]]

def Datetime.IsWf.YYYY (s : String) : Prop :=
  IsFixedDigits 4 s

def Datetime.IsWf.MM (s : String) : Prop :=
  IsFixedDigits 2 s

def Datetime.IsWf.DD (s : String) : Prop :=
  IsFixedDigits 2 s

def Datetime.IsWf.Date (s : String) : Prop :=
  ∃ yyyy mm dd,
    ((s = yyyy ++ "-" ++ mm ++ "-" ++ dd ∧ Datetime.IsWf.YYYY yyyy) ∧ Datetime.IsWf.MM mm) ∧ Datetime.IsWf.DD dd

def Datetime.IsWf.hh (s : String) : Prop :=
  IsFixedDigits 2 s

def Datetime.IsWf.mm (s : String) : Prop :=
  IsFixedDigits 2 s

def Datetime.IsWf.ss (s : String) : Prop :=
  IsFixedDigits 2 s

def Datetime.IsWf.Time (s : String) : Prop :=
  ∃ hh mm ss, ((s = hh ++ ":" ++ mm ++ ":" ++ ss ∧ Datetime.IsWf.hh hh) ∧ Datetime.IsWf.mm mm) ∧ Datetime.IsWf.ss ss

def Datetime.IsWf.SSS (s : String) : Prop :=
  IsFixedDigits 3 s

def Datetime.IsWf.Offset (s : String) : Prop :=
  (∃ hh mm, (s = "+" ++ hh ++ mm ∧ Datetime.IsWf.hh hh) ∧ Datetime.IsWf.mm mm) ∨
    ∃ hh mm, (s = "-" ++ hh ++ mm ∧ Datetime.IsWf.hh hh) ∧ Datetime.IsWf.mm mm

def Datetime.IsWf.Datetime (s : String) : Prop :=
  (((Datetime.IsWf.Date s ∨
          ∃ date time, (s = date ++ "T" ++ time ++ "Z" ∧ Datetime.IsWf.Date date) ∧ Datetime.IsWf.Time time) ∨
        ∃ date time sss,
          ((s = date ++ "T" ++ time ++ "." ++ sss ++ "Z" ∧ Datetime.IsWf.Date date) ∧ Datetime.IsWf.Time time) ∧
            Datetime.IsWf.SSS sss) ∨
      ∃ date time offset,
        ((s = date ++ "T" ++ time ++ offset ∧ Datetime.IsWf.Date date) ∧ Datetime.IsWf.Time time) ∧
          Datetime.IsWf.Offset offset) ∨
    ∃ date time sss offset,
      (((s = date ++ "T" ++ time ++ "." ++ sss ++ offset ∧ Datetime.IsWf.Date date) ∧ Datetime.IsWf.Time time) ∧
          Datetime.IsWf.SSS sss) ∧
        Datetime.IsWf.Offset offset

def Datetime.value (yyyy : String) (mm : String) (dd : String) (time_hh : String) (time_mm : String) (ss : String)
    (sss : String) (offset_hh : String) (offset_mm : String) (offset : String) :=
  epochMillis yyyy mm dd time_hh time_mm ss sss offset_hh offset_mm offset

def Datetime.Constraints (mm : String) (time_hh : String) (time_mm : String) (ss : String) (offset_hh : String)
    (offset_mm : String) (yyyy : String) (dd : String) : Prop :=
  (((((((1 : Int) ≤ natOf mm ∧ natOf mm ≤ (12 : Int)) ∧ (0 : Int) ≤ natOf time_hh ∧ natOf time_hh ≤ (23 : Int)) ∧
            (0 : Int) ≤ natOf time_mm ∧ natOf time_mm ≤ (59 : Int)) ∧
          (0 : Int) ≤ natOf ss ∧ natOf ss ≤ (59 : Int)) ∧
        (0 : Int) ≤ natOf offset_hh ∧ natOf offset_hh ≤ (23 : Int)) ∧
      (0 : Int) ≤ natOf offset_mm ∧ natOf offset_mm ≤ (59 : Int)) ∧
    dayBound yyyy mm dd = true

def Datetime.SatisfiesConstraints (s : String) : Prop :=
  Datetime.Constraints (FormatSpec.component Datetime.grammar s "MM")
    (FormatSpec.component Datetime.grammar s "Time.hh") (FormatSpec.component Datetime.grammar s "Time.mm")
    (FormatSpec.component Datetime.grammar s "ss") (FormatSpec.component Datetime.grammar s "Offset.hh")
    (FormatSpec.component Datetime.grammar s "Offset.mm") (FormatSpec.component Datetime.grammar s "YYYY")
    (FormatSpec.component Datetime.grammar s "DD")

abbrev Datetime.IsValid (s : String) : Prop :=
  Datetime.IsWf.Datetime s ∧ Datetime.SatisfiesConstraints s

-- ══════════════════════════════ engine ══════════════════════════════
-- The analyzable/executable machinery behind the spec: the deep-embedded
-- value/constraint ASTs and the decode-backed interpreter bundle (`isWf`,
-- `isValid`, `computeValue`).

def Datetime.valueFn := fun env : Env =>
  epochMillis (((env : Env) "YYYY").getD "") (((env : Env) "MM").getD "") (((env : Env) "DD").getD "")
    (((env : Env) "Time.hh").getD "") (((env : Env) "Time.mm").getD "") (((env : Env) "ss").getD "")
    (((env : Env) "SSS").getD "") (((env : Env) "Offset.hh").getD "") (((env : Env) "Offset.mm").getD "")
    (((env : Env) "Offset").getD "")

def Datetime.constraints : List ConstraintEntry :=
  [ConstraintEntry.dsl
      (Constraint.and (Constraint.le (ValExpr.lit 1) (ValExpr.nat "MM"))
        (Constraint.le (ValExpr.nat "MM") (ValExpr.lit 12))),
    ConstraintEntry.dsl
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Time.hh"))
        (Constraint.le (ValExpr.nat "Time.hh") (ValExpr.lit 23))),
    ConstraintEntry.dsl
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Time.mm"))
        (Constraint.le (ValExpr.nat "Time.mm") (ValExpr.lit 59))),
    ConstraintEntry.dsl
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "ss"))
        (Constraint.le (ValExpr.nat "ss") (ValExpr.lit 59))),
    ConstraintEntry.dsl
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Offset.hh"))
        (Constraint.le (ValExpr.nat "Offset.hh") (ValExpr.lit 23))),
    ConstraintEntry.dsl
      (Constraint.and (Constraint.le (ValExpr.lit 0) (ValExpr.nat "Offset.mm"))
        (Constraint.le (ValExpr.nat "Offset.mm") (ValExpr.lit 59))),
    ConstraintEntry.opaque fun env : Env =>
      dayBound (((env : Env) "YYYY").getD "") (((env : Env) "MM").getD "") (((env : Env) "DD").getD "")]

abbrev Datetime.isWf (s : String) : Prop :=
  FormatSpec.isWf Datetime.grammar Datetime.constraints s

abbrev Datetime.satisfiesConstraints (s : String) : Prop :=
  FormatSpec.satisfiesConstraints Datetime.grammar Datetime.constraints s

abbrev Datetime.isValid (s : String) : Prop :=
  Datetime.isWf s ∧ Datetime.satisfiesConstraints s

def Datetime.computeValue (s : String) :=
  FormatSpec.computeValueF Datetime.grammar Datetime.valueFn s

-- ════════════════════════════ soundness ════════════════════════════
-- The guarantees tying the two together: the surface⟺engine equivalence
-- `IsWf_equiv` (+ its `Internal.matchesRef.*` lemmas) and the derived
-- `DecidablePred IsWf.*` instance (an executable validator, via the interpreter).

theorem Datetime.Internal.matchesRef.YYYY (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "YYYY") s ↔ Datetime.IsWf.YYYY s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "YYYY" =
        some (Production.mk "YYYY" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 4)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.YYYY
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.MM (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "MM") s ↔ Datetime.IsWf.MM s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "MM" =
        some (Production.mk "MM" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.MM
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.DD (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "DD") s ↔ Datetime.IsWf.DD s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "DD" =
        some (Production.mk "DD" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.DD
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.Date (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 2) (Sym.ref "Date") s ↔ Datetime.IsWf.Date s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "Date" =
        some
          (Production.mk "Date"
            [[SymItem.mk (Sym.ref "YYYY") false, SymItem.mk (Sym.lit "-") false, SymItem.mk (Sym.ref "MM") false,
                SymItem.mk (Sym.lit "-") false, SymItem.mk (Sym.ref "DD") false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.Date
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym,
    Datetime.Internal.matchesRef.YYYY, Datetime.Internal.matchesRef.MM, Datetime.Internal.matchesRef.DD]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.hh (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "hh") s ↔ Datetime.IsWf.hh s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "hh" =
        some (Production.mk "hh" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.hh
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.mm (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "mm") s ↔ Datetime.IsWf.mm s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "mm" =
        some (Production.mk "mm" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.mm
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.ss (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "ss") s ↔ Datetime.IsWf.ss s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "ss" =
        some (Production.mk "ss" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 2)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.ss
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.Time (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 2) (Sym.ref "Time") s ↔ Datetime.IsWf.Time s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "Time" =
        some
          (Production.mk "Time"
            [[SymItem.mk (Sym.ref "hh") false, SymItem.mk (Sym.lit ":") false, SymItem.mk (Sym.ref "mm") false,
                SymItem.mk (Sym.lit ":") false, SymItem.mk (Sym.ref "ss") false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.Time
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym,
    Datetime.Internal.matchesRef.hh, Datetime.Internal.matchesRef.mm, Datetime.Internal.matchesRef.ss]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.SSS (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 1) (Sym.ref "SSS") s ↔ Datetime.IsWf.SSS s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "SSS" =
        some (Production.mk "SSS" [[SymItem.mk (Sym.term TokClass.digit (LenSpec.exactly 3)) false]])
      from rfl]
  dsimp only
  rw [matchesProd_single]
  unfold Datetime.IsWf.SSS
  simp (config := { maxSteps := 1000000 }) only [FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_left,
    if_true, if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, IsDigits_matchesTerm,
    IsFixedDigits_matchesTerm, IsDigitsBetween_matchesTerm]
  simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left, ← and_assoc,
    exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
  try grind [String.append_assoc, String.append_empty]

theorem Datetime.Internal.matchesRef.Offset (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 2) (Sym.ref "Offset") s ↔ Datetime.IsWf.Offset s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "Offset" =
        some
          (Production.mk "Offset"
            [[SymItem.mk (Sym.lit "+") false, SymItem.mk (Sym.ref "hh") false, SymItem.mk (Sym.ref "mm") false],
              [SymItem.mk (Sym.lit "-") false, SymItem.mk (Sym.ref "hh") false, SymItem.mk (Sym.ref "mm") false]])
      from rfl]
  dsimp only
  unfold matchesProd Datetime.IsWf.Offset
  simp (config := { maxSteps := 1000000 }) only [List.mem_cons, List.mem_singleton, List.not_mem_nil,
    FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_or_imp, exists_eq_left, exists_eq_left, if_true,
    if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym, Datetime.Internal.matchesRef.hh,
    Datetime.Internal.matchesRef.mm]
  repeat'
    first
    | apply or_congr
    | ( simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left,
          ← and_assoc, exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
        try grind [String.append_assoc, String.append_empty])

theorem Datetime.Internal.matchesRef.Datetime (fuel : Nat) (s : String) :
    matchesSym Datetime.grammar (fuel + 3) (Sym.ref "Datetime") s ↔ Datetime.IsWf.Datetime s :=
  by
  rw [matchesSym,
    show
      (Datetime.grammar).prod? "Datetime" =
        some
          (Production.mk "Datetime"
            [[SymItem.mk (Sym.ref "Date") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit "Z") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.lit "Z") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.ref "Offset") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.ref "Offset") false]])
      from rfl]
  dsimp only
  unfold matchesProd Datetime.IsWf.Datetime
  simp (config := { maxSteps := 1000000 }) only [List.mem_cons, List.mem_singleton, List.not_mem_nil,
    FormatSpec.matchesSeq.eq_1, FormatSpec.matchesSeq.eq_2, exists_eq_or_imp, exists_eq_left, exists_eq_left, if_true,
    if_false, Bool.false_eq_true, false_and, or_false, or_assoc, FormatSpec.matchesSym,
    Datetime.Internal.matchesRef.Date, Datetime.Internal.matchesRef.Time, Datetime.Internal.matchesRef.SSS,
    Datetime.Internal.matchesRef.Offset]
  repeat'
    first
    | apply or_congr
    | ( simp (config := { maxSteps := 1000000 }) only [String.append_assoc, String.append_empty, exists_and_left,
          ← and_assoc, exists_eq_left, exists_eq_left', exists_eq_right, and_true, Option.some.injEq, forall_eq']
        try grind [String.append_assoc, String.append_empty])

theorem Datetime.IsWf_equiv (s : String) : IsWf Datetime.grammar s ↔ Datetime.IsWf.Datetime s :=
  by
  rw [isWf_eq_isWfProd_start, IsWfProd,
    show
      (Datetime.grammar).prod? (Datetime.grammar).start =
        some
          (Production.mk "Datetime"
            [[SymItem.mk (Sym.ref "Date") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit "Z") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.lit "Z") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.ref "Offset") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.ref "Offset") false]])
      from rfl]
  have hstart :
    ∀ n,
      matchesProd Datetime.grammar n
          (Production.mk "Datetime"
            [[SymItem.mk (Sym.ref "Date") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit "Z") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.lit "Z") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.ref "Offset") false],
              [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.ref "Offset") false]])
          s =
        matchesSym Datetime.grammar (n + 1) (Sym.ref "Datetime") s :=
    by
    intro n
    rw [matchesSym,
      show
        (Datetime.grammar).prod? "Datetime" =
          some
            (Production.mk "Datetime"
              [[SymItem.mk (Sym.ref "Date") false],
                [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                  SymItem.mk (Sym.lit "Z") false],
                [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                  SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.lit "Z") false],
                [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                  SymItem.mk (Sym.ref "Offset") false],
                [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
                  SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false,
                  SymItem.mk (Sym.ref "Offset") false]])
        from rfl]
  show
    matchesProd Datetime.grammar (Datetime.grammar).prods.length
        (Production.mk "Datetime"
          [[SymItem.mk (Sym.ref "Date") false],
            [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
              SymItem.mk (Sym.lit "Z") false],
            [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
              SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.lit "Z") false],
            [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
              SymItem.mk (Sym.ref "Offset") false],
            [SymItem.mk (Sym.ref "Date") false, SymItem.mk (Sym.lit "T") false, SymItem.mk (Sym.ref "Time") false,
              SymItem.mk (Sym.lit ".") false, SymItem.mk (Sym.ref "SSS") false, SymItem.mk (Sym.ref "Offset") false]])
        s ↔
      _
  rw [hstart]
  exact Datetime.Internal.matchesRef.Datetime _ s

instance Datetime.instDecidableIsWf : DecidablePred Datetime.IsWf.Datetime := fun s =>
  @decidable_of_iff _ _ (Datetime.IsWf_equiv s) (FormatSpec.decIsWf Datetime.grammar (by decide) s)

instance Datetime.instDecidableSatisfiesConstraints : DecidablePred Datetime.SatisfiesConstraints := fun s => by
  simp only [Datetime.SatisfiesConstraints, Datetime.Constraints]; exact inferInstance

instance Datetime.instDecidableIsValid : DecidablePred Datetime.IsValid := fun s => inferInstanceAs (Decidable (_ ∧ _))

theorem Datetime.IsValid_equiv (s : String) : Datetime.IsValid s ↔ Datetime.isValid s :=
  by
  unfold Datetime.IsValid Datetime.isValid Datetime.isWf Datetime.satisfiesConstraints
  unfold FormatSpec.isWf FormatSpec.satisfiesConstraints
  rw [← Datetime.IsWf_equiv, ← decodeSome_iff_IsWf Datetime.grammar (by decide)]
  unfold Datetime.SatisfiesConstraints Datetime.Constraints Datetime.constraints
  simp only [FormatSpec.component, List.forall_mem_cons, List.forall_mem_singleton, List.not_mem_nil, forall_const,
    if_true, if_false, ConstraintEntry.wfPart, ConstraintEntry.valPart, Constraint.wfPart, Constraint.valPart,
    Constraint.isValueDependent, Constraint.eval, ValExpr.eval, presentCount, natOf_getD, intOf_getD, lenOf_getD,
    signOf_getD, and_true, true_and, false_implies, implies_true, Bool.false_eq_true]
  try grind

theorem Datetime.computeValue_eq (s : String) :
    Datetime.computeValue s =
      (decode Datetime.grammar s).map
        (fun _ =>
          Datetime.value (FormatSpec.component Datetime.grammar s "YYYY") (FormatSpec.component Datetime.grammar s "MM")
            (FormatSpec.component Datetime.grammar s "DD") (FormatSpec.component Datetime.grammar s "Time.hh")
            (FormatSpec.component Datetime.grammar s "Time.mm") (FormatSpec.component Datetime.grammar s "ss")
            (FormatSpec.component Datetime.grammar s "SSS") (FormatSpec.component Datetime.grammar s "Offset.hh")
            (FormatSpec.component Datetime.grammar s "Offset.mm") (FormatSpec.component Datetime.grammar s "Offset")) :=
  by
  unfold Datetime.computeValue FormatSpec.computeValueF FormatSpec.component FormatSpec.envOf Datetime.value
    Datetime.valueFn
  cases h : decode Datetime.grammar s with
  | none => simp
  | some m => simp only [Option.map_some, natOf_getD, intOf_getD, lenOf_getD, signOf_getD, ValExpr.eval]
